export const metadata = { title: "Faction Application" };

export default function FactionApplicationPage() {
  return (
    <main className="page">
      <div className="container">
        <div className="eyebrow">Applications</div>
        <h1 style={{ fontSize: "clamp(32px,5vw,52px)" }}>Faction Application</h1>
        <p className="lead" style={{ marginTop: 12 }}>
          The faction application form isn't wired up yet — this page is a placeholder. Replace this content once
          the form is ready.
        </p>
      </div>
    </main>
  );
}
